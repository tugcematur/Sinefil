@model Sinefil.Models.Viewmodels.FilmModel

@{
    ViewBag.Title = "Add Movie";
}

<center><p class="head">Add Movie</p></center>


<div class="row">
    <div class="container d-flex justify-content-center align-items-center">
        <div class="col-md-4">
           @*  @if (ViewBag.Errors != null)
            {
                <ul>
                    @foreach (var error in (List<string>)ViewBag.Errors)
                    {
                        <li class="text-danger">@error</li>
                    }
                </ul>
            } *@

            <form method="post" id="filmForm">
                <div class="form-group">
                    <label asp-for="Title"></label>
                    <input asp-for="Title" class="form-control" />
                    <span asp-validation-for="Title" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Director"></label>
                    <input asp-for="Director" class="form-control" />
                    <span asp-validation-for="Director" class="text-danger"></span>

                </div>
                <div class="form-group">
                    <label asp-for="Year"></label>
                    <input asp-for="Year" class="form-control" />
                    <span asp-validation-for="Year" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Genre"></label>
                    <input asp-for="Genre" class="form-control" />
                    <span asp-validation-for="Genre" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="Description"></label>
                    <textarea asp-for="Description" class="form-control" style="height:300px;"></textarea>
                    <span asp-validation-for="Description" class="text-danger"></span>
                </div>
                <div class="form-group">
                    <label asp-for="ImgUrl"></label>
                    <input asp-for="ImgUrl" class="form-control" />
                    <span asp-validation-for="ImgUrl" class="text-danger"></span>
                  @*   <input type="text" id="imgUrl" name="ImgUrl" placeholder="Resim URL'si" class="form-control"/>
                    <span id="errorMsg" style="color:red;"></span><br /> *@
                </div>
                <div class="form-group d-flex justify-content-end  mt-2">
                    <input type="submit" class="btn btn-primary btn-size" value="OK" />
                </div>
                

            </form>

        </div>

    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}


@* <script>
    document.getElementById('filmForm').addEventListener('submit', function(event) {
      const imgUrl = document.getElementById('imgUrl').value.trim();
      const errorMsg = document.getElementById('errorMsg');
      errorMsg.textContent = '';

      if (imgUrl) {
        const pattern = /\.(jpg|jpeg|png|gif|bmp)$/i;
        if (!pattern.test(imgUrl)) {
          errorMsg.textContent = "Geçerli bir resim URL'si giriniz (jpg, png, gif, bmp uzantılı).";
          event.preventDefault();
        }
      }
    });
</script> *@